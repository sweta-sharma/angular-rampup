Steps to run this app:

1. clone git repo:
       git clone git@github.com:sweta-sharma/angular-rampup.git
2. set up database.yml with user and password
3. set up databse:
       rake db:create
       rake db:migrate
4. run rails server
       rails s
5. Go to localhost:3000 on a browser.
